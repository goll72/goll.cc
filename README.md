goll.cc
=======

This is the "source code" for my website, written using lume.land.

To compile it (why would you do that?), you will need to copy the `fonts`
directory as well as `dist/katex.css` from the `katex` npm package over to
`assets`, install `deno` and then run:

```sh
$ deno task serve
```
